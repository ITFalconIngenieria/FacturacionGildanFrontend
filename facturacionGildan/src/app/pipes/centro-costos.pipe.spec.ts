import { CentroCostosPipe } from './centro-costos.pipe';

describe('CentroCostosPipe', () => {
  it('create an instance', () => {
    const pipe = new CentroCostosPipe();
    expect(pipe).toBeTruthy();
  });
});
